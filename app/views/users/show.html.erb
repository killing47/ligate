<div class="contribution">
  <%if current_user.image_id == nil %>
    <%= image_tag(current_user.user_image,  :size => "95x95") %>
  <% else%>
    <%= attachment_image_tag  current_user, :image, format: 'jpeg', size: "95x95" , id:"post"%>
  <% end %>
  <h3>名前を表示</h3>
<%= form_with(model: @current_user) do |f| %>
<%= f.text_field :nick_name %><%= f.submit %>
<% end %>

<%= render "partial/qr_code" %>
<%= link_to "PDF", "http://localhost:3000/users/#{@user.id}.pdf" %>
<%= link_to "PDF", "https://fadfdfafa.herokuapp.com/users/#{@user.id}.pdf" %>

<text_field></text_field>
<div class="slider">

</div>

<form>

</form>

</div>

<style type="text/css">

.contribution{
  margin-top: 100px;
}
</style>
